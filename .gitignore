# build output and caches
/dist/
/.cache/

# local-only
/.env
*.token
/infra/.terraform/
/infra/*.tfstate*
/bench/node_modules/

# wrangler local state
/.wrangler/

# built binary (go build -o site ./cmd/site)
/site

# Claude Code worktrees (the .claude/ skills and CLAUDE.md themselves are public, spec §13)
/.claude/worktrees/
